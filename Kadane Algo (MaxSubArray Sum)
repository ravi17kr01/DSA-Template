int maxSubarraySum(int arr){

    int max_so_far = arr[0];
    int current_max = arr[0];
    
    for(int i=0; i<arr.size(); i++){
          current_max = max(arr[i], curent_max + arr[i]);
          max_so_far = max(max_so_far,current_max);
          
    }
    
    return max_so_far;
    
}
